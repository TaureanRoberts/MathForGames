#include <iostream>
#include "math.h"

void Add(int lhs[], int rhs[], int results)
{
	int addNum = results;
	int i = 0;
	int j = 1;
	results = ((lhs[i] * rhs[i]) + (lhs[j] * rhs[j]));
}

int main()
{
	int i[2] = {1,0};
	int j[2] = {0,1};
	system("pause");
}